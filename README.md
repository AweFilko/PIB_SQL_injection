# PIB_SQL_injection
Project about prevention from SQL injection with experiment

Vulnerable application and Secured application without database, in order for them to work, localdatabase needs to be set
